
To Run the application:
open the command prompt in this path. And run the command as "npm install"

Once installation is completed, you can run the application by this command : "ng serve" / "ng s"